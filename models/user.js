class User {
    constructor(id, ownerId, name, location) {
        this.id = id;
        this.ownerId = ownerId;
        this.name = name;
        this.location = location;
    }
}

export default User;